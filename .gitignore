# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Environment / secrets
.env
.env.*
!.env.example

# Logs and runtime output
*.log
logs/
tmp/
.cache/

# Dependencies and build output (stack not yet chosen; covers the likely candidates)
node_modules/
dist/
build/
out/
target/
__pycache__/
.venv/
venv/

# Local code index (per-checkout, never shared)
.codegraph/

# Runtime checkpoint state (this project's own, never the watched harness tree)
.data/

# Skill registry local cache
.atl/.skill-registry.cache.json

# Gentle AI per-change runtime instance id (machine-local, not shared state)
.gentle-ai-instance
